[project]
name = "enketo-k8s"
version = "0.1.0"
description = "Juju charm: enketo-k8s"
readme = "../../README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.12"
dependencies = ["ops[tracing]>=3.0,<4.0"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
